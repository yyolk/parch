# {{ starter }} starter

Emitted by `parch init --template` (copier). Press with:

```shell
parch press parch.toml -o out.pdf
```

Starters: nomad, scribe, extras, bujo, projects, engineering, steno.
Interactive copier (not the wrapper): `copier copy gh:yyolk/parch dest`.
