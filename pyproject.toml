[project]
name = "planner"
version = "0.0.0"
description = "Throwaway ReportLab yearly planner spike (not parch)."
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.12"
authors = [
  { name = "spike", email = "spike@example.invalid" },
]
keywords = ["e-ink", "planner", "reportlab", "pdf"]
dependencies = [
  "reportlab>=4.2",
]

[project.optional-dependencies]
dev = ["pypdf>=5.0"]

[project.scripts]
press = "planner.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/planner"]
