# Config cookbook

Stub. One handwritten recipe so the Pages layer has a real download — not a dump of the README, and not a generated export of every `examples/*.toml`.

## Nomad 2026 year planner

Sealed Nomad walk: cover → year → quarters → months → weeks → days. Same shape as the default `parch press` with no spec argument.

<a href="../downloads/nomad.toml" download="nomad.toml">Download nomad.toml</a>

```shell
parch press nomad.toml -o nomad-2026.pdf
```

The specimen-bridge stub fills in the link below (today: Pages catalog at the site root; later: `/parch/specimens/` once this tree is the root).

<a data-specimen="supernote-nomad/">Nomad specimens</a>
